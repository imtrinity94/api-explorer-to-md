# Ignore everything
*

# Allow directories so we can look for .md files inside them
!*/

# Explicitly ignore unwanted directories
node_modules/
chrome_profile/

# Allow .md files
!*.md

# Allow .gitignore itself
!.gitignore
